{
  "name": "bpost-landing-pages",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "build": "gulp bundle",
    "clean": "gulp clean",
    "test": "gulp test",
    "watch": "gulp",
    "preinstall": "npx npm-force-resolutions",
    "gulp2": "gulp",
    "eleventy": "npx @11ty/eleventy --serve",
    "localtunnel": "npx localtunnel --subdomain bpost --port 8080",
    "start": "run-p watch eleventy localtunnel"

  },
  "dependencies": {
    "gulp-deploy-azure-cdn": "2.3.0"
  },
  "devDependencies": {
    "async": "^3.2.4",
    "azure-storage": "^2.10.7",
    "browser-sync": "^2.26.13",
    "glob-parent": "^6.0.2",
    "graceful-fs": "^4.2.10",
    "gulp": "^4.0.2",
    "gulp-convert-encoding": "^2.1.0",
    "gulp-sass": "^5.1.0",
    "mime": "^3.0.0",
    "npm-run-all": "^4.1.5",
    "readable-stream": "^4.2.0",
    "sass": "^1.55.0"
  }
}
